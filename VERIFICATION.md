# حالة التحقق — الإصدار 1.1.1 — 6 سبتمبر 2026

## نجح

- تثبيت الاعتمادات المثبتة بالإصدار، مع `package-lock.json` للتثبيت المتكرر.
- مجموعة `npm test`: **76 اختبارًا ناجحًا**، دون تسجيل دخول إلى Discord. شُغلت في هذا التحديث مباشرةً بواسطة `node --test` على ملفات الاختبار الخمسة.
- `npm run check`: الفحص النحوي لجميع ملفات المصدر والاختبارات والسكربتات.
- تهيئة عملاء `discord.js` و`discord.js-selfbot-v13` محليًا، وتوليد تعريفات الأوامر والـEmbeds.
- اختبار حدود منتصف الليل السعودي، بداية الأسبوع والشهر وتغير السنة.
- اختبار استقلال الأعضاء، ثبات اختيار المهام، القوالب الخاصة، والاحتفاظ بنقاط اليوم السابق.
- اختبار منع تكرار المكافآت، حد تكرار المهمة، وفلترة الرسائل.
- اختبار الدقائق المجزأة، الانتقال بين الرومات، الميوت والديفن وعدد البشر.
- اختبار انقطاع القارئ أثناء حفظ الصوت وعدم استعادة حالة صوت قديمة.
- اختبار تعديل أسعار الحضور دون إعادة تسعير النقاط السابقة.
- اختبار خوارزمية مقارنة إصدار الوثيقة بمحاكي مجموعة ذرية، بما فيه التحديث المتزامن وتكرار حدث الإكمال.
- اختبار لوحة setup: الأزرار الأربعة، الأمر الكتابي والدقائق العربية، صلاحيات الإدارة، القنوات، والمدة.
- اختبار التجديد في موعده، استعادة الجدول بعد restart، وتعويض رسالة لوحة حذفت يدويًا.
- اختبار فشل الإرسال/الحفظ/حذف السابقة، والتحقق من نتيجة حفظ غير مؤكدة، ومنع تضاعف الإرسال مع نداءات المؤقت المتزامنة.
- اختبار عدم حذف رسائل شخص آخر أو رسالة بوت لا تحمل أزرار اللوحة.
- اختبار خصوصية مهامي، صحة تصنيف كل زر توب، أزرار الفترات والصفحات ومعرفاتها الفريدة، وبقاء المعالجات صالحة دون Collector في الذاكرة.
- اختبار الحفاظ على التوكن الصحيح، وتصحيح اقتباسات النسخ وبادئة Authorization، ورفض المحارف الخفية والتوكنين المتطابقين.
- اختبار تحديد اتصال البوت والقارئ، ورفض Gateway بالرمز 4004، وفصل خطأ intents عن انتهاء المهلة وتنظيف مستمعي تسجيل الدخول.
- اختبار التشخيص بردود HTTP مصطنعة: 200 و401 و403 و429 و503، ونوع الحساب غير المطابق، وفشل الشبكة والمهلة.
- اختبار منع التحويلات في طلب التشخيص، وعدم إعادة المحاولات أو الاستمرار بعد منع/حد طلبات، وعدم طباعة التوكن أو بيانات الحساب أو محتوى الأخطاء.
- `npm audit --omit=dev` في التحقق السابق لم يبلغ عن ثغرات معروفة وقت الفحص؛ لم تتغير الاعتمادات في 1.1.1. هذا ليس ضمانًا أمنيًا أو ضمان توافق للمكتبة المؤرشفة.

## لم يكتمل

`npm run test:db` حاول تشغيل MongoDB محليًا مؤقتًا بإصدار 8.2.6 لاختبار الفهارس والحفظ والتزامن والترتيب على خادم فعلي. أعادت بيئة التنفيذ `open: Operation not permitted` عند بدء `mongod` وانتهى بالرمز 100. توقف هذا الفحص عند القيد؛ لم تُجر محاولة لتجاوزه. اختبار التكامل مرفق لتشغيله في بيئة تسمح بالخادم المحلي، ولا يستخدم قاعدة بيانات الإنتاج أو بيانات اعتمادها.

## يحتاج إعدادك واختبارًا مباشرًا

- توكن البوت الرسمي وتوكن حساب القارئ وIDs السيرفرات والقنوات والرتبة، في Render Environment فقط.
- نتيجة `npm run diagnose` من بيئة Render؛ اختبارات التشخيص هنا استخدمت ردودًا مصطنعة ولا تثبت قبول توكنك لدى Discord أو أن رفض Gateway قد زال.
- اتصال MongoDB الخاص بالمشروع.
- تسجيل الأوامر فعليًا على Discord.
- تفعيل Message Content Intent للبوت الرسمي، وتجربة setup الكتابي والسلاش والأزرار وتجديد الرسالة في سيرفر الكلان.
- وصول أحداث رسائل وصوت أرينا إلى حساب القارئ، خاصة الرومات التي لا يوجد فيها الحساب صوتيًا.
- النشر المستمر على Render ودورة إعادة التشغيل ثم أول منتصف ليل سعودي.

لم نُنشئ خدمة مدفوعة أو مستودعًا أو قاعدة بيانات بحساباتك، ولم نستخدم أي توكنات من المحادثات السابقة. لا يتضمن الملف أسرارًا أو `node_modules` أو خادم MongoDB الثنائي.
